const chai = require('chai');

describe('REMINDERS, when done with testing', () => {
	describe('comment out:', () => {
		it('send-sms')
		it('missionChooser: auto-fetch Grace Hopper')
	})

	describe('comment in:', () => {
		it('whichMessage.CHALLENGE_ANSWER: voice and image')
	})
})